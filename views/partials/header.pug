header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube
        .header__lolumn
            form(action=routes.search, method="get")
                input(type="text", plasceholder="search", name="term")
    .header__column
        ul
            li
                a(href=routes.join) Join
            li  
                a(href=routes.login) Log In
